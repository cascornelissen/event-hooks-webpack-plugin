# Examples
This directory includes some examples on how `event-hooks-webpack-plugin` can be used. To run an example, clone this
repository, navigate to the example's directory and execute the following commands.

```shell
npm clean-install && \
npm run build && \
npm exec -- webpack
```
